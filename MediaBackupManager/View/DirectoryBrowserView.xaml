<UserControl x:Class="MediaBackupManager.View.DirectoryBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaBackupManager.View"
             xmlns:viewmodel="clr-namespace:MediaBackupManager.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="100" 
             Unloaded="UserControl_Unloaded">
    <Control.Resources>
        <!--Treeview Resources-->
        <Style TargetType="TreeViewItem">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=Name}" Value="Fiddler">
                    <Setter Property="Foreground" Value="BlueViolet" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TreeViewItem" x:Key="BackupStatusColor">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=BackupStatus}" Value="False">
                    <Setter Property="Foreground" Value="Red" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <!--GridView Resources-->
        <Style TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="HorizontalContentAlignment" Value="Left" />
        </Style>
        <CollectionViewSource x:Key="DirectoryFiles" Source="{Binding Path=CurrentDirectory.Files}"/>
        <CollectionViewSource x:Key="DirectorySubDirs" Source="{Binding Path=CurrentDirectory.SubDirectories}"/>
        <CompositeCollection x:Key="CombinedCollection">
            <CollectionContainer Collection="{Binding Source={StaticResource DirectorySubDirs}}" />
            <CollectionContainer Collection="{Binding Source={StaticResource DirectoryFiles}}" />
        </CompositeCollection>
        <Style TargetType="{x:Type ListViewItem}">
            <EventSetter Event="MouseDoubleClick" Handler="gridFiles_MouseDoubleClick" />
        </Style>
    </Control.Resources>
    
    <DockPanel x:Name="MainDock" LastChildFill="True" Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Height="30">
            <Button x:Name="btnClearData" Content="Clear" Command="{Binding ClearDataCommand}"></Button>
            <Button x:Name="btnLoadData" Content="Load DB" Command="{Binding LoadData}"></Button>
            <Button x:Name="btnScanFiles" Content="Scan Test 1" Command="{Binding LoadAdditionalData}"></Button>
            <Button x:Name="btnScanAdditional" Content="Scan Test 2" Command="{Binding ScanNewData}"></Button>
            <Button x:Name="btnDeleteAdditional" Content="Delete Test 2" Command="{Binding RemoveNewData}"></Button>
            <Button x:Name="btnCreateBackupSet" Content="Index Directory" Command="{Binding CreateBackupSetCommand}"></Button>
            <Button x:Name="btnRemoveBackupSet" Content="Remove Directory" Command="{Binding RemoveBackupSetCommand}"></Button>
        </StackPanel>

        <!--<TreeView x:Name="treeDirectory" Margin="2" DockPanel.Dock="Left" MinWidth="50" MaxWidth="250"
                  ItemsSource="{Binding Path=Index.BackupSets}"
                  SelectedItemChanged="treeDirectory_SelectedItemChanged">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type viewmodel:BackupSetViewModel}" 
                                          ItemsSource="{Binding Path=RootDirectory.SubDirectories}">
                    <TreeViewItem Header="{Binding Path=RootDirectory.FullName}"/>
                </HierarchicalDataTemplate>
                <HierarchicalDataTemplate DataType="{x:Type viewmodel:FileDirectoryViewModel}" 
                                          ItemsSource="{Binding Path=SubDirectories}">
                    <TreeViewItem Header="{Binding Path=Name}" Style="{StaticResource BackupStatusColor}"/>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>-->

        <TreeView x:Name="treeDirectory" Margin="2" DockPanel.Dock="Left" MinWidth="50" MaxWidth="250"
                  ItemsSource="{Binding Path=Index.BackupSets}"
                  SelectedItemChanged="treeDirectory_SelectedItemChanged">
            <TreeView.Resources>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="IsExpanded" Value="{Binding TreeViewIsExpanded}" />
                    <Setter Property="IsSelected" Value="{Binding TreeViewIsSelected}" />
                    <!--<Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="FontWeight" Value="Bold" />
                        </Trigger>
                    </Style.Triggers>-->
                </Style>
                <HierarchicalDataTemplate DataType="{x:Type viewmodel:BackupSetViewModel}" 
                                          ItemsSource="{Binding Path=RootDirectory.SubDirectories}">
                    <TreeViewItem Header="{Binding Path=RootDirectory.FullName}"/>
                </HierarchicalDataTemplate>
                <HierarchicalDataTemplate DataType="{x:Type viewmodel:FileDirectoryViewModel}" 
                                          ItemsSource="{Binding Path=SubDirectories}">
                    <TreeViewItem Header="{Binding Path=Name}" Style="{StaticResource BackupStatusColor}"/>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>


        <ListView x:Name="gridFiles" Margin="2" DockPanel.Dock="Right"
                  SelectedItem="{Binding Path=SelectedFileGridItem}"
                  ItemsSource="{StaticResource CombinedCollection}">
            <ListView.Resources>

            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Extension" DisplayMemberBinding="{Binding Extension}" />
                    <GridViewColumn Header="Checksum" DisplayMemberBinding="{Binding Checksum}" />
                    <GridViewColumn Header="Backup Status" DisplayMemberBinding="{Binding BackupStatus}" />
                    <GridViewColumn Header="Backup Count" DisplayMemberBinding="{Binding Path=Hash.BackupCount}" />
                    <GridViewColumn Header="Node Count" DisplayMemberBinding="{Binding Path=Hash.NodeCount}" />
                    <GridViewColumn Header="Length" DisplayMemberBinding="{Binding Path=Hash.Length}" />
                    <GridViewColumn Header="Creation Time" DisplayMemberBinding="{Binding Path=Hash.CreationTime}" />
                    <GridViewColumn Header="Last Write Time" DisplayMemberBinding="{Binding Path=Hash.LastWriteTime}" />
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</UserControl>

<UserControl x:Class="MediaBackupManager.View.CreateBackupSetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaBackupManager.View"
             xmlns:popups="clr-namespace:MediaBackupManager.View.Popups"
             xmlns:popupsvm="clr-namespace:MediaBackupManager.ViewModel.Popups"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Control.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="2" />
        </Style>

        
        <!--<DataTemplate DataType="{x:Type popupsvm:YesNoPopupViewModel}">
            <popups:YesNoPopup/>
        </DataTemplate>-->
        
    </Control.Resources>
    <DockPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10">
        <DockPanel.Style>
            <Style TargetType="DockPanel">
                <!--Disable all controls while a popup is active-->
                <Setter Property="IsEnabled" Value="True" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding ShowPopup}" Value="True">
                        <Setter Property="IsEnabled" Value="False" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </DockPanel.Style>
            <Popup IsOpen="{Binding ShowPopup}">
            <ContentControl Content="{Binding Path=CurrentPopup}" />
        </Popup>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <TextBox Width="200" Height="40" IsEnabled="True" Text="{Binding BackupSetLabel}"></TextBox>
        </StackPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <TextBox Width="200" Height="40" IsReadOnly="True" Text="{Binding Path=SelectedDirectory}"></TextBox>
            <Button x:Name="btnSelectDirectory" Content="Select Directory" Command="{Binding SelectDirectoryCommand}"></Button>
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Vertical" Margin="5">
            <TextBox Text="{Binding ScanStatusText}" Width="230"  />
            <ProgressBar Height="20" Width="230"
                         Minimum="0" Maximum="100" 
                         Value="{Binding ScanProgress}"></ProgressBar>
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Height="25" Margin="5">
            <Button x:Name="btnConfirm" Content="OK" Command="{Binding ConfirmCommand}"></Button>
            <Button x:Name="btnCancel" Content="Cancel" Command="{Binding CancelCommand}"></Button>
        </StackPanel>
    </DockPanel>
</UserControl>

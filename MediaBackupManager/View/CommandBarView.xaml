<UserControl x:Class="MediaBackupManager.View.CommandBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaBackupManager.View"
             xmlns:model="clr-namespace:MediaBackupManager.Model"
             xmlns:viewmodel="clr-namespace:MediaBackupManager.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300">
    
    <StackPanel Orientation="Horizontal" Height="30">
        <Button x:Name="btnClearData" Content="Clear" Command="{Binding ClearData}"></Button>
        <Button x:Name="btnLoadData" Content="Load DB" Command="{Binding LoadData}"></Button>
        <Button x:Name="btnScanFiles" Content="Scan Test 1" Command="{Binding LoadAdditionalData}"></Button>
        <Button x:Name="btnScanAdditional" Content="Scan Test 2" Command="{Binding ScanNewData}"></Button>
        <Button x:Name="btnDeleteAdditional" Content="Delete Test 2" Command="{Binding RemoveNewData}"></Button>
        <Button x:Name="btnCreateBackupSet" Content="Index Directory" Command="{Binding CreateBackupSet}"></Button>
    </StackPanel>
</UserControl>
